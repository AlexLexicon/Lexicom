<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <LangVersion>11</LangVersion>

    <Description>Simplifies creating Roslyn source generators at the cost of performance.</Description>
    <PackageTags>sourcegenerator;analyzers</PackageTags>
  </PropertyGroup>

  <ItemGroup>
    <!--Allows this project to be used as a nuget package analyzer-->
    <None Include="$(OutputPath)\$(AssemblyName).dll" Pack="true" PackagePath="analyzers/dotnet/cs" Visible="false" />
  </ItemGroup>

  <ItemGroup>
    <!--Last i checked this always requires version '3.3.4' to work-->
    <PackageReference Include="Microsoft.CodeAnalysis.Analyzers" Version="5.3.0" PrivateAssets="all" />
    <!--Last i checked this always requires version '4.5.0' to work-->
    <PackageReference Include="Microsoft.CodeAnalysis.CSharp" Version="5.3.0" PrivateAssets="all" />
  </ItemGroup>

</Project>
